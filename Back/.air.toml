root = "."
tmp_dir = "tmp"

[build]
  cmd = "go build -o ./tmp/server ."
  bin = "./tmp/server"
  include_ext = ["go"]
  exclude_dir = ["tmp"]
  delay = 500
  poll = true
  poll_interval = 1000

[log]
  time = false

[misc]
  clean_on_exit = true
